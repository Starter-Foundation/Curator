{
  "name": "curator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The DM's partner",
  "scripts": {
    "build": "esbuild src/script.js --bundle --outfile=script.js --format=iife",
    "build:watch": "esbuild src/script.js --bundle --outfile=script.js --format=iife --watch"
  },
  "dependencies": {
    "@neondatabase/auth": "^0.5.0-beta",
    "@neondatabase/neon-js": "^0.7.0-beta",
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.8.0",
    "dompurify": "^3.4.15",
    "jose": "^6.2.12",
    "marked": "^16.4.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
